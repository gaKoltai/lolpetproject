import React from "react";
import styled from "styled-components";

const StyledQueueStatsWrapper = styled.div`
    direction: flex;
    flex-direction: row;
`;

interface Props {}

const QueueStats = (props: Props) => {
    return <div></div>;
};

export default QueueStats;
